# Create a basic requirements.txt file as a starting point
requirements_content = """
flask==2.2.3
numpy==1.25.0
pandas==2.1.3
"""

# Save the requirements.txt file in the extracted project folder
requirements_path = os.path.join(extracted_path, 'requirements.txt')
with open(requirements_path, 'w') as file:
    file.write(requirements_content)

requirements_path
